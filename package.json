{
  "name": "intelligenciaartificial-cat",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/server dist/.openai dist/public && cp -R public/. dist/ && cp -R public/. dist/public/ && cp server/index.js dist/server/index.js && cp .openai/hosting.json dist/.openai/hosting.json",
    "dev": "npx serve public -l 4173",
    "publish:daily": "node scripts/publish-daily.mjs"
  },
  "dependencies": {
    "openai": "^6.0.0"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
